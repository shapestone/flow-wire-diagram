# Wide Character Test

A diagram containing a wide emoji character (visual width 2).
The `-w` flag should detect it; repair should not silently corrupt the content.

```ascii
┌──────────────────┐
│  Status: OK      │
│  Flag:   🌍      │
└──────────────────┘
```
