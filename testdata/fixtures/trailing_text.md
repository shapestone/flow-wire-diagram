# Trailing Text Test

Boxes with descriptive text past the right edge.

```ascii
┌──────────────┐
│ API Layer    │ - HTTP handlers, request/response
└──────┬───────┘
       │
┌──────▼───────┐
│ Data Layer   │ - Database access, queries
└──────────────┘
```
